interface:
  display_name: "Test UI"
  short_description: "Run planned console UI tests and record a transcript"
  default_prompt: "Use $test-ui to run the console UI test cases in test/ui-test-plan.md."
